{
  "name": "contract-secretary",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client:start": "pnpm --filter client start",
    "client:android": "pnpm --filter client android",
    "client:ios": "pnpm --filter client ios",
    "client:web": "pnpm --filter client web",
    "server:start": "pnpm --filter server start",
    "server:dev": "pnpm --filter server start:dev",
    "server:build": "pnpm --filter server build",
    "prisma:generate": "pnpm --filter server prisma:generate",
    "prisma:migrate": "pnpm --filter server prisma:migrate",
    "prisma:migrate:dev": "pnpm --filter server prisma:migrate:dev",
    "prisma:studio": "pnpm --filter server prisma:studio",
    "prisma:push": "pnpm --filter server prisma:push",
    "prisma:pull": "pnpm --filter server prisma:pull",
    "docker:up": "docker-compose --env-file .env up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "docker:rebuild": "docker-compose down -v && docker-compose --env-file .env build --no-cache && docker-compose --env-file .env up -d"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
